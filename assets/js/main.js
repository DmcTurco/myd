/* =============================================
   MYDSAC — Punto de entrada JS
   (carga los módulos: navbar.js, hero-slider.js,
    companies-carousel.js, form-validation.js)
   ============================================= */
document.addEventListener('DOMContentLoaded', () => {
    initNavbar();
    initHeroSlider();
    initCompaniesCarousel();
    initFormValidation();
    initContactoMap();
});
